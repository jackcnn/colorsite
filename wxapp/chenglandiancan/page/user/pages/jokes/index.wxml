<view class="container">
    <view wx:for="{{list}}" wx:key="{{this}}" class="content">
        <view class="name">{{item.title}}</view>
        <view class="desc"><text>{{item.desc}}</text></view>
    </view>
    <view bindtap="getdata" class="loading">{{loading?'加载中...':'加载更多'}}</view>
</view>